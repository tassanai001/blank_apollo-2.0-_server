import { ExecutionResult } from 'graphql';
export interface PersistedQueryCache {
    set(key: string, data: string): Promise<any>;
    get(key: string): Promise<string | null>;
}
export declare type HttpHeaderCalculation = (responses: Array<ExecutionResult & {
    extensions?: Record<string, any>;
}>) => Record<string, string>;
export declare function calculateCacheControlHeaders(responses: Array<ExecutionResult & {
    extensions?: Record<string, any>;
}>): Record<string, string>;
