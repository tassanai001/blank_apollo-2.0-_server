/// <reference types="node" />
import * as http from 'http';
import { ApolloServer as ApolloServerBase } from 'apollo-server-express';
export { GraphQLOptions, GraphQLExtension, gql } from 'apollo-server-core';
export * from './exports';
export interface ServerInfo {
    address: string;
    family: string;
    url: string;
    port: number | string;
    subscriptionsPath: string;
    server: http.Server;
}
export declare class ApolloServer extends ApolloServerBase {
    private httpServer;
    private createServerInfo(server, subscriptionsPath?);
    listen(...opts: Array<any>): Promise<ServerInfo>;
    stop(): Promise<void>;
}
